{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "paths": {
            "@centroidjs/web/router": [
                "../router/dist/index"
            ],
            "@centroidjs/web": [
                "../dist/index"
            ]
        } // (build) don't use paths provided by the root tsconfig
    },
    "include": [
        "src"
    ],
    "exclude": [
        "node_modules",
        "dist",
        "spec"
    ]
}